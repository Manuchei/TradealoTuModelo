/*
Crea una función llamada filterExchanges que reciba como parámetros una lista de exchanges(array), un name(string) y un trust_score(number).
La función debe devolver un array con los exchanges que cumplan las siguientes condiciones:
- El name del exchange debe contener el name pasado como parámetro
- El trust_score del exchange debe ser mayor que el trust_score pasado como parámetro
Si a la función se le pasa un parametro name que sea falsy, no filtrará por name
Si a la función se le pasa un parametro trust_score que se falsy, no filtrará por trust_score


La lista de exchanges (array) que se le pasará será parecida a la que está almacenada en test/exchangesMock.json
 */
export function filterExchanges(lista, name, trust_score) {
  // <<<<Tu código aquí>>>>
}
